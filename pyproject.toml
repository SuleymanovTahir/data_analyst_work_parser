[project]
name = "data-analyst-work-parser"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.117.1",
    "requests>=2.32.0",
    "vercel>=0.2.0",
]

[project.scripts]
app = "hh_vacancy_bot:app"
